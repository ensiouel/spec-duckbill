# Execution Clarification

Implementation may choose local details left discretionary by plan. Return clarification only when a material unknown prevents safe preflight or apply.

Use owner `specification` when different answers change observable behavior, public contracts, data/security obligations, acceptance, or outcomes. Use owner `plan` when they change architecture, components, dependencies, task design, or proof approach.

Return 1-5 focused questions in the standard form with stable `Q-###` IDs, reason, question, and options. Make no writes and do not ask the user directly.
