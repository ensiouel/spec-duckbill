# Plan and Task Refinement

Apply only feedback owned by technical approach or task decomposition. Compare the complete proposal with specification before any write. If it weakens, broadens, or contradicts product intent, return blocked and write neither artifact.

Update plan.md, tasks.md, or both as required. Preserve unrelated architecture, mappings, tasks, IDs, and ordering. Change a stable task ID only when its identity changes. Keep plan free of state and tasks free of execution metadata.

Return all suggested affected task IDs. Include changed definitions, actions, checks, mappings, dependencies, retired work, and current affected work. The deterministic runtime computes its own set and transitive dependents, then unions suggestions.
