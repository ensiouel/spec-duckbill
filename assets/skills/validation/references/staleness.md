# Freshness

Use results produced during the current validation when practical. Do not treat an old report, a completed task label, or a claim in documentation as proof that current code passes.

When the specification, plan, task definition, or relevant implementation changed, rerun the affected checks.
