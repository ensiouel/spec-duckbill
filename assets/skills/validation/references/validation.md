# Validation

For task validation, evaluate every CHK item of the selected task against current code and current check output.

For feature validation, evaluate every VAL item against the approved specification, plan, completed tasks, implementation, and current project checks.

Report each item exactly once as passed, failed, or unavailable. A check passes only when current observable results support it. Do not repair failures during validation.
