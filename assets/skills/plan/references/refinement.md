# Plan and Task Refinement

Apply only feedback owned by technical approach or task decomposition. Compare the complete proposal with specification before any write. If it weakens, broadens, or contradicts product intent, return blocked and write neither artifact.

Update plan.md, tasks.md, or both as required. Preserve unrelated architecture, mappings, tasks, IDs, ordering, and completed status. Change a stable task ID only when its identity changes. Keep runtime logs out of both files.

Report affected task IDs. Include changed definitions, actions, checks, mappings, dependencies, retired work, and dependent tasks that must return to `pending`.
