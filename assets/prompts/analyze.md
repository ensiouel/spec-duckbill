# Analyze

Analyze only the supplied scope. For `spec`, decide whether the specification is clear enough to plan. For `all`, check agreement between constitution, specification, plan, tasks, and current implementation.

Make no writes. Report concrete findings with file references and suggested ownership: specification, plan, tasks, or code.
