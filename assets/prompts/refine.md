# Refine

Apply the supplied feedback only within the selected scope.

For `spec`, write only spec.md. For `plan`, write only plan.md and tasks.md. For `code`, repair only the selected task and update its status after running relevant checks. Preserve unrelated user changes and do not start another action.
