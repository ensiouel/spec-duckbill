# Validate feature

Evaluate the current implementation against the complete approved hierarchy.

Make no writes. Use normal read and shell tools, run relevant project checks, evaluate every feature validation item, and report passed, failed, or unavailable checks with concise reasons.
