# Execute task

Implement and validate exactly the selected task.

Read the specification, plan, and tasks first. Execute only the requested task after confirming its dependencies are completed. Change only application files needed by that task, run its checks with normal Pi tools, and set its `**Status:**` to `completed` only when the checks pass. Stop after one task.
