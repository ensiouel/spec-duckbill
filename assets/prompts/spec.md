# Create specification

Develop the supplied draft into a ready WHAT-and-WHY specification. Treat extra command arguments as optional context.

Write only canonical spec.md. Ask focused questions when product intent is missing. Mark it `ready` only when it is internally clear and complete. Do not create the plan or start the next action.
